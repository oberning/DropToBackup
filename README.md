# DropToBackup

A simple GUI tool that copies a file that is dragged and dropped in the drop zone to a (backup) folder with a timestamp prefix, e.g. in the format yyyymmdd-HHMMSS_\<original filename and extension\>

## Installation
Execute as follows in the folder:
```bash
pip install .
```

